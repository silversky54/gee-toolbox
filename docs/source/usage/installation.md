# Installation

Install the latest release from PyPI:

```bash
pip install gee-toolbox
```

For local development from a clone of this repository:

```bash
uv sync
```

## Requirements

- Python 3.13 or newer
- A Google Earth Engine account. User needs to authenticate the Earth Engine Python client
  (`earthengine authenticate`, or credentials configured for your environment)
